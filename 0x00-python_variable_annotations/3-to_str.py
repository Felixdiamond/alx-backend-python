#!/usr/bin/env python3
"""This module defines a type-annotated function to_str that takes a float as an argument and returns its string representation."""


def to_str(n: float) -> str:
    """Takes a float n as argument and returns its string representation.

    Args:
        n: A float.

    Returns:
        The string representation of n.
    """
    return str(n)
